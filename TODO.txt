* Move binary files onto a server and add a python script for downloading the files to a local hard-drive
* Trophic status as a new (composite) variable? (trophic status is calculated from some input and intermediate variables, so it's an engineered feature) - NOT DONE AT THIS POINT. DECIDED TO MOVE AWAY FROM CALCULATED FEATURES (FEATURE ENGINEERING) AND USE ONLY SELECTED SUBSET OF RAW INPUTS
